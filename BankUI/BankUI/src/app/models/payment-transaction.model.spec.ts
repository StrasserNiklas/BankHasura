import { PaymentTransaction } from './payment-transaction.model';

describe('PaymentTransaction', () => {
  it('should create an instance', () => {
    expect(new PaymentTransaction()).toBeTruthy();
  });
});
